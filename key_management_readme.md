# JWT密钥管理功能

本系统实现了安全的JWT密钥管理功能，包括自动密钥轮换和手动密钥管理工具。

## 密钥管理特性

- **自动密钥轮换**: 系统每30天自动轮换一次JWT密钥，增强安全性
- **密钥备份机制**: 每次轮换前自动备份当前密钥文件
- **权限控制**: 自动设置密钥文件的安全权限（在Linux/Unix系统上）
- **错误恢复**: 提供日志记录和错误处理，确保密钥管理稳定性
- **管理工具**: 提供命令行工具用于密钥状态检查和手动轮换

## 管理工具使用

### 检查密钥状态

使用以下命令检查当前JWT密钥的状态：

```bash
python check_key_status.py
```

该命令会显示以下信息：
- 上次密钥轮换时间
- 下次计划轮换时间
- 距离下次轮换的天数
- 密钥文件权限状态
- 密钥备份信息

### 手动轮换密钥

如需手动轮换密钥（例如在安全事件后），可使用以下命令：

```bash
python rotate_key.py
```

如果当前密钥未过期但仍需强制轮换，可使用：

```bash
python rotate_key.py --force
```

**注意**: 密钥轮换后需要重启应用才能使新密钥生效。

## 安全建议

1. **保护密钥文件**: 确保`.env`文件和`last_key_rotation.txt`文件的访问权限设置正确
2. **定期备份**: 定期备份`keybackups`目录以防密钥丢失
3. **安全审计**: 定期使用`check_key_status.py`工具检查密钥状态
4. **敏感场景轮换**: 在管理员变更等敏感场景下，考虑使用`rotate_key.py`手动轮换密钥

## 密钥文件位置

- 主密钥文件: `.env` (包含SECRET_KEY配置)
- 轮换记录: `last_key_rotation.txt`
- 密钥备份: `keybackups/`目录 